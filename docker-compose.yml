services:
  neo4j:
    image: neo4j:5.26.28-community
    volumes:
        - .data:/data         
    environment:
        - NEO4J_AUTH=neo4j/${PASSWORD}
    ports:
      - "7474:7474"
      - "7687:7687"
    restart: always